<Window x:Class="Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid Margin="0,0,0,0">
        <Grid Name="MenuGrid" HorizontalAlignment="Stretch" Height="35" Margin="0,0,0,0" VerticalAlignment="Top"/>
        <Grid Name="HeaderGrid" HorizontalAlignment="Stretch" Height="35" Margin="0,35,0,0" VerticalAlignment="Top">
            <Button x:Name="ProjectsButton" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,0" Width="35" Height="35" Click="ProjectsButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="./icons/project.png" Margin="0,0,0,0" Stretch="Fill" IsEnabled="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </StackPanel>
            </Button>
        </Grid>
        <Grid Name="ContentGrid" HorizontalAlignment="Stretch" Margin="0,70,0,35" VerticalAlignment="Stretch">

        </Grid>
        <Grid Name="FooterGrid" HorizontalAlignment="Stretch" Height="35" Margin="0,0,0,0" VerticalAlignment="Bottom"/>

    </Grid>

</Window>
